对象创建型模式：

	创建型模式抽象了实例化过程，帮助一个系统独立于对象如何创建，组合和表示它的那些对象。
	一个类创建型模式使用<span style="color:red">继承</span>改变被实例化的类，
	<span style="coloc:red">将实例化委托给另一个对象</span>。
	
	
	也就是说从原先的直接new一个对象，也就是硬编码变成了更灵活的创建。when,where,how,
	what to 都是可以配置的，这样就把对象的创建与对象的使用耦合解放出来。